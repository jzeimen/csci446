%h1 Listing users

%table
  %tr
    %th Username
    %th Email
    %th Password
    %th First
    %th Last
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.username
      %td= user.email
      %td= user.password
      %td= user.first
      %td= user.last
      %td= link_to 'Show', [:member, user]
      %td= link_to 'Edit', edit_member_user_path(user)
      %td= link_to 'Destroy', [:member,user], :confirm => 'Are you sure?', :method => :delete


